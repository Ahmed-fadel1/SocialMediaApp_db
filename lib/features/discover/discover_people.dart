import 'package:flutter/material.dart';

class DiscoverPeople extends StatelessWidget {
  const DiscoverPeople({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Discover People"));
  }
}
